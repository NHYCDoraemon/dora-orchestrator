[project]
name = "dora-orchestrator"
version = "0.1.0"
description = "Dagster-first orchestration platform for Dora-style autonomous engineering workflows"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dagster = ["dagster>=1.8"]

[tool.setuptools.packages.find]
include = ["dora_orchestrator*"]
